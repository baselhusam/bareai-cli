.nh
.TH "bareai" "1" "Jan 0001" "Auto generated by spf13/cobra" ""

.SH NAME
bareai-mcp - Run MCP server for coding agents (stdio)


.SH SYNOPSIS
\fBbareai mcp [flags]\fP


.SH DESCRIPTION
Start a Model Context Protocol server on stdin/stdout so coding agents
(Cursor, Claude Desktop, etc.) can inspect this box via bareai tools.

.PP
Activity logs (startup, initialize, tool calls) go to stderr; stdout is reserved for MCP protocol traffic.

.PP
Use --fifo-dir for local two-terminal testing with scripts/mcp-server-fifo.sh and
scripts/mcp-smoke.sh --attach. Cursor and Claude Desktop use plain stdio (no flag).
See docs/agents.md for setup examples.


.SH OPTIONS
\fB--fifo-dir\fP=""
	Unix only: read/write MCP via named pipes at DIR/in and DIR/out (for local smoke testing)

.PP
\fB-h\fP, \fB--help\fP[=false]
	help for mcp


.SH OPTIONS INHERITED FROM PARENT COMMANDS
\fB-j\fP, \fB--json\fP[=false]
	output in JSON format

.PP
\fB--no-color\fP[=false]
	disable colored output

.PP
\fB--timeout\fP=10s
	timeout for probes and API calls


.SH EXAMPLE
.EX
  bareai mcp
  bareai mcp --fifo-dir /tmp/bareai-mcp
  # Cursor MCP config: { "command": "bareai", "args": ["mcp"] }
.EE


.SH SEE ALSO
\fBbareai(1)\fP


.SH HISTORY
1-Jan-0001 Auto generated by spf13/cobra
